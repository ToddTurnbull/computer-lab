# computer-lab

```bash
# Git
sudo apt update
sudo apt upgrade -y
sudo apt install -y --no-upgrade git

git config --global user.name "Todd Turnbull"
git config --global user.email "shingaling@gmail.com"

mkdir ~/git
```
